# ===== NoLSAF (example env) =====
# Copy to .env for local dev OR configure these in Vercel/Render/AWS.

# --- API (Render) ---
NODE_ENV=production
PORT=4000
HOST=0.0.0.0

# Database (AWS RDS MySQL / MariaDB)
# Example: mysql://USER:PASSWORD@HOST:3306/DBNAME?connection_limit=10
DATABASE_URL=

# Redis (Render Redis / Upstash / etc)
REDIS_URL=

# Encryption (required for production)
# Generate via (either):
#   cd apps/api && npm run generate:encryption-key
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
ENCRYPTION_KEY=

# Auth (required for production)
# Used to sign/verify JWTs (cookies + Socket.IO auth). In dev a fallback is allowed, but prod must set this.
JWT_SECRET=

# Optional: cookie domain to share auth cookies across subdomains.
# Set this if you use a custom domain like:
#   web: https://app.example.com
#   api: https://api.example.com
# Then set: COOKIE_DOMAIN=.example.com
COOKIE_DOMAIN=

# CORS allow-list (comma-separated origins). This should include your Vercel frontend domain(s).
# Example: https://your-app.vercel.app,https://www.yourdomain.com
CORS_ORIGIN=

# Optional explicit origins used by adminOriginGuard
WEB_ORIGIN=
APP_ORIGIN=

# --- Web (Vercel) ---
# Used by Socket.IO client in the browser
NEXT_PUBLIC_SOCKET_URL=

# Used by some client code as a fallback; if you rely on Next rewrites you can still set this.
NEXT_PUBLIC_API_URL=

# Used by Next.js rewrites (server-side). On Vercel set this to your Render service URL.
# Example: https://nolsaf-api.onrender.com
API_ORIGIN=

# --- Uploads (optional, depending on your setup) ---
# Cloudinary (used by /api/uploads/cloudinary/sign)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# AWS S3 (used by /api/uploads/s3/presign)
AWS_REGION=
S3_BUCKET=

# Email (optional)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=

# --- Geocoding (Mapbox) ---
# Required by the API reverse/forward geocoding routes.
# Prefer setting this (server-side) for the API runtime:
MAPBOX_ACCESS_TOKEN=
# Some code paths also read this fallback name; set it only if you intentionally share the same token:
NEXT_PUBLIC_MAPBOX_TOKEN=
