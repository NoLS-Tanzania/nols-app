# ===== NoLSAF (example env) =====
# Copy to .env for local dev OR configure these in Vercel/Render/AWS.

# --- API (Render) ---
NODE_ENV=production
PORT=4000
HOST=0.0.0.0

# Database (AWS RDS MySQL / MariaDB)
# Example: mysql://USER:PASSWORD@HOST:3306/DBNAME?connection_limit=10
DATABASE_URL=

# Redis (Render Redis / Upstash / etc)
REDIS_URL=

# Encryption (required for production)
# Generate via: cd apps/api && npm run generate:encryption-key
ENCRYPTION_KEY=

# CORS allow-list (comma-separated origins). This should include your Vercel frontend domain(s).
# Example: https://your-app.vercel.app,https://www.yourdomain.com
CORS_ORIGIN=

# Optional explicit origins used by adminOriginGuard
WEB_ORIGIN=
APP_ORIGIN=

# --- Web (Vercel) ---
# Used by Socket.IO client in the browser
NEXT_PUBLIC_SOCKET_URL=

# Used by some client code as a fallback; if you rely on Next rewrites you can still set this.
NEXT_PUBLIC_API_URL=

# Used by Next.js rewrites (server-side). On Vercel set this to your Render service URL.
# Example: https://nolsaf-api.onrender.com
API_ORIGIN=
